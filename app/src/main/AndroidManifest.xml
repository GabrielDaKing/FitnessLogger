<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="gncis.com.example.android.fitnesslogger">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainMenu">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".ExcersizeActivity"/>
        <activity android:name=".ExcersizeCreate"/>
        <activity android:name=".ExcersizeDisplay"/>
        <activity android:name=".IntervalTimerActivity"/>
        <activity android:name=".UserActivity"/>
        <activity android:name=".WorkoutActivity"/>
        <activity android:name=".WorkoutCreate"/>
        <activity android:name=".WorkoutDisplay"/>
    </application>
</manifest>
